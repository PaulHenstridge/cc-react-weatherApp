const CityMap = ({ city }) => {

    // so in this function i need to ge tthe coordinates form the city passed in
    // pass them to a createMap function
    // render the map in the return statement

    return (<h2>[a map will go here]</h2>);

}

export default CityMap;